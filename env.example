# Index Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Embedding Configuration
# =============================================================================

# Option 1: SiliconFlow (Chinese models, recommended for Chinese text)
# SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# Option 2: OpenAI
# OPENAI_API_KEY=your_openai_api_key_here

# Option 3: Local (no API key needed, uses sentence-transformers)
# If no API key is provided, local embedding will be used automatically

# Embedding model (optional, defaults based on provider)
# EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5    # For SiliconFlow
# EMBEDDING_MODEL=text-embedding-3-small     # For OpenAI
# EMBEDDING_MODEL=all-MiniLM-L6-v2           # For local

# =============================================================================
# Storage Configuration
# =============================================================================

# Storage provider: "chroma" (default)
# STORAGE_PROVIDER=chroma

# Storage path for ChromaDB
# STORAGE_PATH=./data/chroma

# Collection name
# COLLECTION_NAME=index_kb

# =============================================================================
# Chunking Configuration
# =============================================================================

# Chunk size (characters)
# CHUNK_SIZE=500

# Chunk overlap (characters)
# CHUNK_OVERLAP=50
